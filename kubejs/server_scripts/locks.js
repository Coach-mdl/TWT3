const locksRecipes = (event) => {};

const locksData = (event) => {};

const locksItemTags = (event) => {};

const locksBlockTags = (event) => {
  event.add("locks:lockable", "#handcrafted:cupboards");
};
